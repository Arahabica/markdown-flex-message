{
  "name": "markdown-flex-message",
  "version": "0.3.2",
  "description": "markdown-flex-message is a converter that transforms Markdown into Flex Message for the LINE Messaging API.",
  "main": "dist/markdown-flex-message.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "biome format ./src",
    "lint:fix": "biome format --write ./src"
  },
  "author": "Arahabica",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/prismjs": "^1.26.3",
    "@vitest/coverage-v8": "^1.1.1",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  },
  "dependencies": {
    "@line/bot-sdk": "^9.2.0",
    "image-size": "^1.1.0",
    "marked": "^11.2.0",
    "prismjs": "^1.29.0"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "!dist/tests/**/*",
    "LICENSE",
    "README.md"
  ],
  "engines": {
    "node": ">= 18"
  }
}
